Third-party files served with the app, copied unmodified from npm:

- `three/three.module.min.js` and `three/OrbitControls.js` come from three.js r160 (`three@0.160.0`). They are MIT licensed; see `three/LICENSE`.
- `manifold/manifold.js` and `manifold/manifold.wasm` come from manifold 3.5.3 (`manifold-3d@3.5.3`), the WebAssembly build of the CSG library that `fpmb/mesh.py` uses through `manifold3d`. They are Apache 2.0 licensed; see `manifold/LICENSE`.
